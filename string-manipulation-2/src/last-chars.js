/* exported lastChars */

function lastChars(length, string) {
  var answer = string.slice(string.length - length);
  if (length < string.length) {
    // console.log(answer);
    return answer;
  }
  return 'LearningFuze';
}
